spring:
  kafka:
    bootstrap-servers: 127.0.0.1:9092
    template:
      default-topic: game

    consumer:
      auto-offset-reset: earliest

logging:
  level:
    de.igorakkerman.demo.kafka: DEBUG

